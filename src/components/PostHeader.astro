---
import { formatDate } from '../utils/reading-time';

interface Props {
  title: string;
  description?: string;
  date?: Date;
  readingTime?: string;
  lang: 'zh-CN' | 'en';
}

const { title, description, date, readingTime, lang } = Astro.props;
---

<div class="post-header">
  <h1>{title}</h1>
  {(date || readingTime) && (
    <div class="meta">
      {date && (
        <time datetime={date.toISOString()}>
          {formatDate(date, lang)}
        </time>
      )}
      {readingTime && (
        <>
          &bull;
          <span class="reading-time">{readingTime}</span>
        </>
      )}
    </div>
  )}
  {description && <p class="description">{description}</p>}
</div>

<style>
  .post-header {
    margin-bottom: 2rem;
  }
  h1 {
    margin-bottom: 0.5rem;
  }
  .meta {
    display: flex;
    gap: 0.5rem;
    align-items: center;
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-bottom: 1rem;
  }
  time {
    display: inline;
  }
  .reading-time {
    display: inline;
  }
  .description {
    color: var(--text-muted);
    font-size: 1.1rem;
    margin-top: 0.5rem;
  }
</style>
